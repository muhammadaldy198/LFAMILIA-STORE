name: Temporary MLBB Diagnostic

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Probe production Melostore diagnostic
        run: |
          curl -sS -w '\nHTTP_STATUS:%{http_code}\n' \
            'https://lfamiliastore.my.id/api/_diag/mlbb-4f7a91c2'

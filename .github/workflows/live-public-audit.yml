name: Live Public Audit

on:
  pull_request:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Node 24
        uses: actions/setup-node@v7
        with:
          node-version: "24"
      - name: Read-only Cloudflare preview audit
        env:
          PRODUCTION_BASE_URL: https://3c983103-lfamilia-store.muhammadaldy198.workers.dev
        run: node scripts/live-public-audit.mjs
